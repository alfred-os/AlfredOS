name: Nightly

on:
  schedule:
    # 06:00 UTC — drives the e2e + adversarial jobs (gated per-job below).
    - cron: "0 6 * * *"
    # 21:30 UTC — inside DeepSeek's off-peak discount window (16:30–00:30 UTC,
    # ~50–75% cheaper) — drives the real-llm-smoke job ONLY (gated per-job below).
    - cron: "30 21 * * *"
  workflow_dispatch:

# Schedule-driven; do not cancel overlapping runs.
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: false


env:
  # Single source of truth: .prototools. Pinned by tests/unit/test_workflow_invariants.py
  # (#525) — every setup-uv use must consume this, never the action default.
  UV_VERSION: "0.11.32"

permissions:
  contents: read  # Minimal: read repo content for e2e + adversarial runs

jobs:
  e2e:
    name: End-to-end
    runs-on: ubuntu-latest
    # Off-peak split (see `on.schedule`): the 06:00 cron drives e2e + adversarial;
    # the 21:30 cron drives real-llm-smoke only; workflow_dispatch runs all three.
    if: ${{ github.event_name == 'workflow_dispatch' || github.event.schedule == '0 6 * * *' }}
    # No job-level `hashFiles()` guard: GitHub Actions rejects `hashFiles()`
    # in job-level `if:` at workflow-parse time (HTTP 422 "Unrecognized
    # function: 'hashFiles'") — the function is only valid inside step
    # contexts. The first-step `check` below probes the corpus and every
    # downstream step short-circuits on `steps.check.outputs.has_e2e`.
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - name: Check for e2e suite
        id: check
        run: |
          set -euo pipefail
          if [ -f tests/e2e/conftest.py ]; then
            echo "has_e2e=true" >> "$GITHUB_OUTPUT"
          else
            # FAIL-CLOSED (#517, from #514): this path is permanent, so finding nothing means
            # the PROBE broke, not that there is nothing to gate. Skipping would report
            # success while running nothing.
            echo "::error::tests/e2e/conftest.py missing — E2E job cannot run"
            exit 1
          fi
      - uses: astral-sh/setup-uv@c771a70e6277c0a99b617c7a806ffedaca235ff9 # v9.0.0
        with:
          version: ${{ env.UV_VERSION }}
        if: steps.check.outputs.has_e2e == 'true'
      - if: steps.check.outputs.has_e2e == 'true'
        run: uv sync --frozen --dev
      - name: Load the bwrap userns AppArmor profile (#290)
        id: load_apparmor
        if: steps.check.outputs.has_e2e == 'true'
        # docker-compose.yaml pins alfred-core at security_opt: apparmor=alfred-bwrap; Docker
        # REFUSES to create the container without the loaded profile — so without this, core's
        # xfail would fire for an apparmor reason, MASKING blocker B. Guarded on apparmor_parser.
        run: |
          set -euo pipefail
          if command -v apparmor_parser >/dev/null 2>&1; then
            sudo apparmor_parser -r -W docker/apparmor/alfred-bwrap
            echo "Loaded alfred-bwrap AppArmor profile."
          else
            echo "::notice::apparmor_parser absent — skipping profile load (security_opt is a no-op)."
          fi
      - name: Run E2E boot lane (services + core/gateway; setup runs separately)
        if: steps.check.outputs.has_e2e == 'true'
        # No GF-password seed and no provider-key env here: the harness's isolated --env-file
        # owns GF_SECURITY_ADMIN_PASSWORD and the dummy sentinel keys. A shell env var would
        # SHADOW the --env-file via compose ${..} precedence (ops-004). #500 un-xfailed core
        # on this same dummy sentinel: boot-to-healthy needs no real provider key, only a
        # migrated DB + a seeded operator. A real throwaway quarantine key remains a future
        # concern only for tests that exercise an actual provider call, not this boot lane.
        # `-o addopts=''` clears the repo default `-m "not real_llm"` for this lane (a no-op today —
        # no e2e test is real_llm-marked); strict-markers still holds per-PR. `-m 'not
        # e2e_full_setup'` deselects the full setup.sh run — it boots its own postgres and would
        # collide on host port 5432 with the session-scoped boot_stack, so it runs isolated below.
        run: uv run pytest tests/e2e -o addopts='' -m 'not e2e_full_setup'
      - name: Assert the boot-lane tally is non-vacuous
        if: always() && steps.check.outputs.has_e2e == 'true'
        # always(): the conftest hook writes e2e-tally.json even when pytest exits non-zero;
        # a missing file (session errored before the hook) is itself a red (_assert_ran).
        run: uv run python -m tests.e2e._assert_ran e2e-tally.json
      - name: Run the isolated full setup.sh provisioning lane (#501)
        # !cancelled() makes this an independent lane — a boot-lane *test* failure must not skip it
        # (which would leave the setup-assert reding on a missing tally) — but a manual/timeout
        # CANCELLATION does stop it, so it can't keep the runner busy for the whole 60m budget
        # (CodeRabbit). Also gate on the infra prereq (`load_apparmor` succeeded → so did setup-uv
        # + uv sync, since it is not-always and would be skipped otherwise): if the environment
        # setup broke, don't run this lane on a broken env and don't red the setup-assert on a
        # never-written tally. The gate is independent of the boot *pytest* outcome (runs later).
        if: ${{ !cancelled() && steps.check.outputs.has_e2e == 'true' && steps.load_apparmor.conclusion == 'success' }}
        # Its OWN pytest session — never instantiates the session-scoped boot_stack, so
        # setup.sh's `up -d alfred-postgres` (host 5432) does not collide. Distinct tally file
        # via ALFRED_E2E_TALLY_PATH so it does not clobber the boot lane's e2e-tally.json.
        env:
          ALFRED_E2E_TALLY_PATH: e2e-setup-tally.json
        run: uv run pytest tests/e2e -o addopts='' -m 'e2e_full_setup'
      - name: Assert the setup-lane ran (non-vacuity, #245)
        if: ${{ !cancelled() && steps.check.outputs.has_e2e == 'true' && steps.load_apparmor.conclusion == 'success' }}
        run: uv run python -m tests.e2e._assert_ran --setup e2e-setup-tally.json
      - name: Upload diagnostics on failure
        # cancelled() too: the job's own timeout-minutes cancellation reports as `cancelled`, not
        # `failed`, so a lane that overruns its budget (a genuine regression) would otherwise lose
        # its diagnostics exactly when they're most needed (review: devops).
        if: (failure() || cancelled()) && steps.check.outputs.has_e2e == 'true'
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: e2e-diagnostics
          path: |
            e2e-stack.log
            e2e-tally.json
            e2e-setup-tally.json

  adversarial:
    name: Adversarial test suite
    runs-on: ubuntu-latest
    # Runs on the 06:00 cron (or manual dispatch), not the 21:30 off-peak cron.
    if: ${{ github.event_name == 'workflow_dispatch' || github.event.schedule == '0 6 * * *' }}
    # No job-level `hashFiles()` guard (see e2e job above for rationale).
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - name: Check for adversarial corpus
        id: check
        run: |
          set -euo pipefail
          if [ -f tests/adversarial/conftest.py ]; then
            echo "has_adversarial=true" >> "$GITHUB_OUTPUT"
          else
            # FAIL-CLOSED (#517, from #514): this path is permanent, so finding nothing means
            # the PROBE broke, not that there is nothing to gate. Skipping would report
            # success while running nothing.
            echo "::error::tests/adversarial/conftest.py missing — RELEASE-BLOCKING adversarial job cannot run"
            exit 1
          fi
      - uses: astral-sh/setup-uv@c771a70e6277c0a99b617c7a806ffedaca235ff9 # v9.0.0
        with:
          version: ${{ env.UV_VERSION }}
        if: steps.check.outputs.has_adversarial == 'true'
      - if: steps.check.outputs.has_adversarial == 'true'
        run: uv sync --frozen --dev
      - if: steps.check.outputs.has_adversarial == 'true'
        run: uv run pytest tests/adversarial -q

  real-llm-smoke:
    name: Real-LLM act-loop smoke
    runs-on: ubuntu-latest
    # Runs ONLY on the 21:30 UTC cron — DeepSeek's off-peak window (~50–75%
    # cheaper) — or on manual dispatch; skipped on the 06:00 peak cron.
    if: ${{ github.event_name == 'workflow_dispatch' || github.event.schedule == '30 21 * * *' }}
    timeout-minutes: 25
    permissions:
      contents: read  # Minimal: read repo content; testcontainers use the host docker socket.
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - name: Check for the smoke test
        id: check
        run: |
          set -euo pipefail
          if [ -f tests/integration/orchestrator/test_act_loop_real_llm_smoke.py ]; then
            echo "has_smoke=true" >> "$GITHUB_OUTPUT"
          else
            # FAIL-CLOSED (#517, from #514): this path is permanent, so finding nothing means
            # the PROBE broke, not that there is nothing to gate. Skipping would report
            # success while running nothing.
            echo "::error::real-LLM smoke test missing — smoke job cannot run"
            exit 1
          fi
      - uses: astral-sh/setup-uv@c771a70e6277c0a99b617c7a806ffedaca235ff9 # v9.0.0
        with:
          version: ${{ env.UV_VERSION }}
        if: steps.check.outputs.has_smoke == 'true'
      - if: steps.check.outputs.has_smoke == 'true'
        run: uv sync --frozen --dev
      - name: Run the real-LLM smoke (bounded retry to absorb LLM nondeterminism)
        if: steps.check.outputs.has_smoke == 'true'
        # ALFRED_SMOKE_PROVIDER_KEY: a throwaway low-balance deepseek-chat key the
        # operator provisions as a repo secret. Absent (fork PR / unprovisioned)
        # => the test SKIPS (skip-unless-key), never spends. Scoped via env:,
        # never interpolated into run: (workflow-injection guard).
        env:
          ALFRED_SMOKE_PROVIDER_KEY: ${{ secrets.ALFRED_SMOKE_PROVIDER_KEY }}
        # Dep-free bounded retry: 3 attempts, 5s backoff. Matches the spec's
        # `--reruns 2` INTENT (3 total attempts) without adding pytest-rerunfailures
        # (CLAUDE.md: no new dep without justification) and gets fresh
        # testcontainers per attempt (more robust vs an infra/pull flake). A
        # skipped test (no key) exits 0 on attempt 1 → no wasted retries. Every
        # conditional uses `if` so `set -e` never aborts mid-loop. Each attempt
        # is itself bounded by `timeout 300` (5m) so a stuck/degraded provider
        # fails fast (exit 124) instead of the 20-25m job-level timeout firing
        # mid-attempt-1 and starving the retry budget — 3 x 5m attempts fits
        # comfortably inside the 25m job timeout alongside setup/checkout.
        run: |
          set -euo pipefail
          if [ -z "${ALFRED_SMOKE_PROVIDER_KEY:-}" ]; then
            echo "::warning title=real-LLM smoke skipped::ALFRED_SMOKE_PROVIDER_KEY is not provisioned — this nightly run validated NOTHING (paper-gate). Provision the repo secret to activate the smoke."
          fi
          for i in 1 2 3; do
            if timeout 300 uv run pytest \
              tests/integration/orchestrator/test_act_loop_real_llm_smoke.py \
              -m real_llm -v; then
              exit 0
            fi
            echo "::warning::real-LLM smoke attempt ${i}/3 failed"
            if [ "$i" -lt 3 ]; then sleep 5; fi
          done
          exit 1
