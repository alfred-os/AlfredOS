# Required status checks on `main`

This file is the **canonical, version-controlled manifest** of CI checks that must pass before a PR can merge to `main`. The actual enforcement lives in GitHub branch protection (Settings → Branches → `main` → "Require status checks to pass"); this file is the human-readable mirror.

**Why have this file**: branch-protection config is invisible to PRs that should be updating it, not searchable from the repo, and easily out of sync with the YAML that emits the checks. Every change to required checks lands in the same PR as the workflow change that produced them, so the diff is reviewable here.

## How to read the table

- **Check name** — the exact string GitHub matches against the branch-protection list. Comes from the job's `name:` field in the workflow YAML.
- **Workflow** — the file path of the YAML that emits this check.
- **Job key** — the job's YAML key (used in `jobs:`). For navigating the workflow file, not for branch protection.
- **Active since** — the date the check was added to the required list (not the date the workflow was merged).
- **Rationale** — one sentence on why this is required (vs informational).

## How to update this file

When you add a new gating workflow, follow [`.rulesync/skills/author-gating-workflow/SKILL.md`](../../.rulesync/skills/author-gating-workflow/SKILL.md). After the workflow merges and you run `gh api POST .../contexts`, also append a row here in a follow-up PR (or amend the original PR before merge if your branch hasn't been merged yet).

When you remove a required check: same flow in reverse — `gh api -X DELETE .../contexts` and update this file.

## Currently required

| Check name | Workflow | Job key | Active since | Rationale |
|---|---|---|---|---|
| `Conventional commit format` | `.github/workflows/pr-validate-commits.yml` | `conventional-commits` | 2026-05-25 | Every commit subject must follow `<type>[(<scope>)][!]: <description> (#NN)` so the commit log stays parseable and traceable to an issue. |
| `No autosquash commits` | `.github/workflows/pr-validate-commits.yml` | `no-autosquash` | 2026-05-25 | `fixup!` / `squash!` / `amend!` commits must be squashed via `make autosquash` before merge — they're scaffolding, not history. |
| `No merge commits` | `.github/workflows/pr-validate-commits.yml` | `no-merge-commits` | 2026-05-25 | PRs are rebased onto main, not merged. Merge commits in a PR mean someone synced via `git merge` instead of `git rebase`. |
| `Gitleaks (secrets)` | `.github/workflows/pr-validate-security.yml` | `gitleaks` | 2026-05-25 | Block PRs that introduce secrets / credentials / API tokens. Scans the PR diff only (cheap). |
| `Zizmor (workflow security)` | `.github/workflows/pr-validate-security.yml` | `zizmor` | 2026-05-25 | Block PRs that introduce GitHub Actions security issues (workflow injection, unpinned uses, artipacked credentials, excessive token permissions). |
| `Semgrep (SAST)` | `.github/workflows/pr-validate-security.yml` | `semgrep` | 2026-05-25 | Block PRs that introduce findings from the `p/security-audit` + `p/secrets` + `p/github-actions` rulesets. Returns ~0 findings today (no Python code yet); coverage activates as Slice 1+ lands code. |
| `Trivy (IaC + filesystem)` | `.github/workflows/pr-validate-security.yml` | `trivy` | 2026-05-25 | Block PRs that introduce CRITICAL/HIGH vulnerabilities in the filesystem (secrets, license issues, future `uv.lock` deps) or IaC misconfigurations (future Dockerfile / docker-compose / workflow files). |
| `CodeQL (Python)` | `.github/workflows/codeql.yml` | `analyze` | 2026-05-25 | Block PRs that introduce findings from CodeQL's `security-and-quality` query suite (data-flow-sensitive bugs Semgrep's rule-based engine misses). Free for public repos via GitHub Advanced Security. Skips when there's no Python source yet (pre-Slice-1). |
| `Ruff format` | `.github/workflows/pr-validate-python.yml` | `ruff-format` | 2026-06-06 | Enforces `ruff format --check` (formatter no-op) so the formatter is the source of truth, not reviewer time. Was pending since 2026-05-27; promoted alongside the ci.yml gates (#201). |
| `Ruff lint` | `.github/workflows/pr-validate-python.yml` | `ruff-lint` | 2026-06-06 | Enforces `ruff check` with the strict rule set (E, F, I, B, UP, N, S, ARG, RET, SIM, PTH, DTZ, FBT, PIE, RUF). Was pending since 2026-05-27; promoted alongside the ci.yml gates (#201). |
| `Mypy (strict)` | `.github/workflows/pr-validate-python.yml` | `mypy` | 2026-06-06 | Primary type-checker. Enforces `mypy --strict` on `src/`. Was pending since 2026-05-27; promoted alongside the ci.yml gates (#201). |
| `Pyright` | `.github/workflows/pr-validate-python.yml` | `pyright` | 2026-06-06 | Secondary type-checker. Catches data-flow patterns mypy misses. Was pending since 2026-05-27; promoted alongside the ci.yml gates (#201). |
| `Markdown lint` | `.github/workflows/ci.yml` | `markdown` | 2026-06-06 | Documentation linting via `markdownlint-cli2@0.14.0` with the repo-canonical `.markdownlint-cli2.jsonc` config (rules tuned for AlfredOS docs in PR #186). |
| `Python (lint, types, unit)` | `.github/workflows/ci.yml` | `python` | 2026-06-06 | Canonical Python check: ruff lint + ruff format-check + mypy --strict + pyright + unit tests + per-subsystem 100% coverage gates (`security/*`, `hooks/*`, `plugins/*`, `web_fetch/*`). Subsumes the former `Pytest` job from `pr-validate-python.yml` (removed in #201 because it presented a no-coverage parallel green signal that masked CLAUDE.md hard-rule-#7 enforcement debt). |
| `Integration` | `.github/workflows/ci.yml` | `integration` | 2026-06-06 | Integration test suite under `tests/integration/` against real Testcontainers (Postgres, Redis). Appends coverage data to the artifact `coverage-gates` consumes. PR-S4-6: the job now apt-installs `bubblewrap` + `jq` so the merge-blocking `test_launcher_policy_resolver.py` (skipif-bwrap-missing locally) actually exercises the real bwrap sandbox in CI rather than silently skipping. **PR-S4-9 merge-blocking gates** run inside this job — this repo aggregates `tests/integration/` under one `Integration` check rather than emitting a separately-named GitHub context per test (adding per-test contexts that no job emits would block all PRs, per the path-filter caveat below): `tests/integration/test_discord_addressing_modes.py` (the four addressing modes round-trip) and `tests/integration/test_discord_subpayload_promotion.py` (the nine sub-payload kinds promote to a `ContentHandle` against a real Redis `ContentStore`, orchestrator never seeing raw bytes). **PR-S4-10 merge-blocking gate** joins them under the same aggregate: `tests/integration/test_tui_round_trip.py`. Three of its legs gate on CI: a keystroke-batch reaching the host `process_inbound_message` with the `IdentityResolver` consulted exactly once and the canonical id never crossing the wire (`test_tui_inbound_reaches_process_inbound_message`), the peppered T3 audit-row promotion (`test_tui_inbound_records_peppered_audit_row`), and the outbound reply rendering to the real `AlfredTuiApp` RichLog with a non-`dm` mode refused (`test_tui_outbound_renders_to_plugin_richlog` / `test_tui_outbound_refuses_non_dm_addressing_mode`). **The launcher-spawn leg (`test_tui_launcher_spawn_stdio_outbound_round_trip`) does NOT gate on CI — it is best-effort/local.** The TUI plugin declares `sandbox.kind = "none"`, whose launcher path uses `runuser` (root-only); the non-root CI integration runner cannot exec it, so that leg `pytest.skip`s cleanly in CI and only runs on a host with the right privileges (a root container / local dev with `runuser`). Do not read it as a CI-enforced gate — per the repo's "a skipping gate is not a gate" discipline, the stdio-outbound launcher contract is covered locally, not in CI. **PR-S4-11b real-subprocess comms-spawn proof (`tests/integration/cli/daemon/test_daemon_comms_inbound_turn.py`) does NOT gate on CI — same posture as the TUI launcher-spawn leg above.** The reference comms adapter declares `sandbox.kind = "none"`, whose launcher path uses `runuser` (root-only); the non-root `ubuntu-latest` `Integration` runner cannot exec it, so the real-spawn leg `pytest.skip`s cleanly in CI and runs only on macOS dev / a root runner. Do NOT read CI-green as including the PR-S4-11b real-inbound-turn proof. What DOES gate on CI for PR-S4-11b: the daemon boot-wiring **unit** tests (`tests/unit/cli/daemon/test_daemon_comms_spawn.py`, incl. the FIX 4 multi-adapter refusal and the FIX 2 audited corrupt-manifest refusal in `test_daemon_quarantine_boot_infra.py`), the load-grant policy **unit** tests (`tests/unit/security/capability_gate/test_comms_adapter_grants.py`, incl. the FIX 1 tier ceiling), the `.`/`..` traversal validator tests (`tests/unit/config/test_settings_comms_enabled_adapters.py`), and the release-blocking **adversarial corpus** (`cap-2026-003` / `cap-2026-004`) — those run in `Python (lint, types, unit)` / the adversarial suite, not this `Integration` row. A regression in any of the three CI-gating TUI legs fails the `Integration` check and blocks merge. |
| `Coverage gates (combined unit + integration)` | `.github/workflows/ci.yml` | `coverage-gates` | 2026-06-06 | The CLAUDE.md hard-rule-#7 trust-boundary 100% gate. Combines unit + integration coverage and enforces 100% line+branch on `security/*`, `hooks/*`, plugins trust-boundary files, and the web-fetch trust-boundary list. Was paper-only for months because of a broken `coverage combine` invocation that dropped unit data (fixed in #200); promoted because PR #200's fix lets the union actually fire. |
| `Smoke (end-to-end)` | `.github/workflows/ci.yml` | `smoke` | 2026-06-06 | End-to-end TUI smoke against an in-process mock provider (and the real provider when a non-placeholder `ALFRED_SMOKE_PROVIDER_KEY` is configured). Pins the TUI → orchestrator → episodic-write → audit-row contract. **PR-S4-11b real-spawn daemon comms smoke (`tests/smoke/test_slice4_daemon_comms_spawn.py`) does NOT gate on CI** — like the TUI launcher-spawn leg, the reference comms adapter's `sandbox.kind = "none"` launcher path uses `runuser` (root-only), so on the non-root `ubuntu-latest` smoke runner the real-spawn proof `pytest.skip`s and runs only on macOS dev / a root runner. Do not read CI-green as including it; its machinery is gated by the PR-S4-11b boot-wiring unit tests + the `cap-2026-003`/`cap-2026-004` adversarial corpus (see the `Integration` row). |
| `Setup script smoke (ubuntu-latest)` | `.github/workflows/ci.yml` | `setup-script-smoke` | 2026-06-06 | `bin/alfred-setup.sh --dry-run` on ubuntu-latest. Catches bash-portability and prereq-check regressions in the operator-facing setup script. |
| `Setup script smoke (macos-latest)` | `.github/workflows/ci.yml` | `setup-script-smoke` | 2026-06-06 | `bin/alfred-setup.sh --dry-run` on macos-latest. Catches bash 3.2 / BSD-coreutils regressions the ubuntu leg won't see. Uses a no-op `docker` shim (PR #191) so `--dry-run` reaches the bash logic without needing a real daemon. |

## Pending required (workflow merged, awaiting `gh api POST .../contexts`)

These checks are emitted by their workflow but not yet in the branch-protection required list. Per the [author-gating-workflow skill](../../.rulesync/skills/author-gating-workflow/SKILL.md) Step 4: after the workflow merges and runs at least once on a PR, append each name via `gh api`, then move the row to "Currently required" above with today's date.

> **Before promoting `Hook dispatch perf gate` to a required check:**
> The workflow `.github/workflows/perf.yml` uses a `paths:` filter (it only triggers
> when changes touch `src/alfred/hooks/**`, `src/alfred/memory/episodic.py`,
> `tests/perf/**`, `pyproject.toml`, or the workflow file). If promoted to a required
> check as-is, PRs that don't touch those paths would have the check stay in "Pending"
> and the PR would be blocked from merging
> (see [GitHub docs](https://docs.github.com/en/pull-requests/collaborating-with-pull-requests/collaborating-on-repositories-with-code-quality-features/troubleshooting-required-status-checks)).
>
> Mitigation options (decide before promotion):
>
> 1. Add a companion always-triggered workflow that reports the same check name
>    and short-circuits to success on off-path PRs.
> 2. Drop the `paths:` filter from `perf.yml` (lets the workflow run on all PRs;
>    higher CI cost, but simpler).
> 3. Use `actions/changed-files` inside a single job to gate the actual bench run
>    while always reporting the check name.

| Check name | Workflow | Job key | Rationale | Promote after |
|---|---|---|---|---|
| `i18n catalog freshness` | _(to be authored — `.github/workflows/pr-validate-i18n.yml`)_ | _(planned)_ | CLAUDE.md hard rule #4: `pybabel extract` runs in pre-commit; `pybabel compile --check` runs in CI. Catalog drift (extracted msgids missing from `.po`, or `.po` failing to compile) fails the build. Not yet authored — tracked as a Slice 1 follow-up. | After the i18n workflow PR merges. |
| `Hook dispatch perf gate` | `.github/workflows/perf.yml` | `perf` | Slice-2.5 spec §5: release-blocking dispatch-overhead gate for the pluggable-hooks subsystem. Asserts the p99 delta over a per-runner baseline is < 100µs for empty-hookpoint dispatch and < 1ms for a 5-subscriber pre chain (empirical budgets calibrated in `tests/perf/test_hook_dispatch_perf.py`). Workflow carries NO `continue-on-error` — release-blocking from day one. CI invokes `make test-perf` (Makefile is single-source-of-truth for the invocation shape); the workflow sets `ALFRED_TEST_PERF_FORCE=1` so the Makefile's host-load guard (refuses at load ≥ 1.0× CPUs, warns at 0.7×) defers to CI's explicit "runner is fine" signal. Contributors running `make test-perf` locally can set the same env var (`1` / `true` / `yes`, case-insensitive) to skip the guard. **Before promoting:** see the path-filter caveat above the table — `perf.yml`'s `paths:` filter would block off-path PRs unless mitigated. | This PR merges + workflow runs on any subsequent PR. |
| `Integration (privileged Linux, real spawn)` | `.github/workflows/ci.yml` | `integration-privileged` | **Axis 2 — closes #245 Problem 1.** Runs the launcher-spawn / real-bwrap-spawn integration + smoke legs under `sudo` (euid 0) with bubblewrap installed, so the legs that `skipif (not bwrap) or (sysname != Linux) or (geteuid != 0)` on the non-root `ubuntu-latest` runners actually EXECUTE rather than silently skipping. Today these legs are paper-only: PR #243's required-kwarg break on `test_daemon_comms_inbound_turn.py` was green-because-skipped and only surfaced on a local root run. A precondition step asserts euid 0 + bwrap presence before pytest so the job can never go green while gating nothing. **PROPOSED REQUIRED.** Validated locally in `docker run --rm --privileged debian:bookworm` (bubblewrap 0.8.0): `test_comms_runner_substrate.py` un-skips and passes; the two Postgres-testcontainer legs collect cleanly as root (the `_LAUNCHER_REQUIRES_ROOT` skipif does not fire). **#248 closes the deeper paper-gate inside this job: the REAL dual-LLM bwrap quarantine-child spawn (`tests/integration/test_quarantine_child_real_spawn.py`, ADR-0030) previously SKIPPED even under root+bwrap because it ALSO gates on `ALFRED_QUARANTINE_CHILD_PYTHON` (`_PROVISIONED`).** The job now provisions a hermetic, `proto`-managed Python 3.14 under `~/.proto` (NOT a system / deadsnakes `/usr` python) with `alfred` installed into it via `uv pip install --python`, and threads `ALFRED_QUARANTINE_CHILD_PYTHON` into the root pytest run; the launcher binds that `~/.proto` prefix read-only into the sandbox (the opt-in interpreter-prefix bind, ADR-0030) so the wheel-co-located `alfred.security.quarantine_child` resolves off it. The provisioning step asserts the child package imports from that interpreter (fails loud otherwise), and the precondition step asserts the bound interpreter is non-empty + executable + euid 0 + bwrap present, so the real-spawn round-trip RUNS rather than silently skipping. It also runs the **bwrap-gated sandbox-escape adversarial corpus** (`tests/adversarial/sandbox_escape`, incl. `sbx-2026-012` fd-3 key-leak containment + `sbx-2026-013` the live "policy was inert, now enforced" host-escape proof) which `@_bwrap_required`-skip in the dedicated `adversarial` workflow (no bwrap there). Validated in `docker run --rm --privileged --platform linux/amd64 ubuntu:24.04`: `test_quarantine_child_real_spawn.py` RUNS+passes (1 passed, not skipped) and `sbx-2026-012`/`-013` un-skip and pass. Graduation criterion #8 / Slice-4 daemon go-live unblocker. | This PR merges + the job runs green on a subsequent PR (then `gh api POST .../contexts` + move this row to Currently required). |
| `Python cross-OS (macos-latest)` | `.github/workflows/ci.yml` | `python-cross-os` | **Axis 1 — OS breadth.** Runs ruff lint + format + mypy + pyright on macOS. Type-checking is pinned to the Linux deployment target (`mypy --platform linux` / `pyright --pythonplatform Linux`) so the POSIX-only `os` surface the runtime uses (`os.setrlimit`/`RLIMIT_CORE`, `os.writev`, `os.O_NOFOLLOW`) resolves against the stubs it actually runs on, not the host runner's. macOS uses bash 3.2 + BSD coreutils, so this leg is the real portability signal for macOS-specific path / type drift. **Does NOT run the unit suite**: ~77 unit tests under `tests/unit/plugins/web_fetch/`, `.../security/`, `.../config/` boot a real Redis/Postgres via Testcontainers (no `@pytest.mark.integration` — they import `testcontainers` directly), and the GitHub macOS runner ships no Docker daemon, so they ERROR at fixture setup (#245 first real run: 3731 passed / 77 errored). Running the Docker-free unit subset on macOS needs a `requires_docker` marker on the entangled tests — deferred to the #245/#246 follow-up. **PROPOSED REQUIRED** (lint + type layer; give it one or two PR cycles to confirm macOS-runner stability first). | This PR merges + the macOS leg runs green for ~2 PR cycles. |
| `Python cross-OS (windows-latest)` | `.github/workflows/ci.yml` | `python-cross-os` | **Axis 1 — OS breadth.** Runs ruff lint + format + mypy + pyright on Windows (the portable static-analysis layer), type-checked against the Linux target (`--platform linux` / `--pythonplatform Linux`) so the runtime's POSIX-only `os` surface does not raise false `attr-defined` errors on the Windows `os` stubs (#245 first real run flagged 14 such false positives). Does NOT run the unit suite: dozens of unit tests invoke the POSIX bash launcher (`/bin/sh`), `os.uname()`, `os.geteuid()`, `signal.SIGKILL`, and `subprocess(..., pass_fds=...)` with no `sys.platform == "win32"` skip-guard, so they would ERROR (not skip) on Windows. Guarding them is a cross-cutting code change out of scope for this additive CI PR — tracked in #246 (follow-up to #245). **INFORMATIONAL-UNTIL-GREEN, implemented as `continue-on-error: ${{ matrix.os == 'windows-latest' }}`**: a Windows-only regression stays VISIBLE in the PR checklist but does NOT block merge until the leg burns in across a few PRs (type-checkers occasionally diverge on path-separator / line-ending edge cases). Because `continue-on-error` already makes it non-blocking, it is deliberately NOT in the branch-protection required list; promote it to a required check (and drop the `continue-on-error`) once it is proven stable. | The Windows lint/type layer runs green across ~3 PR cycles. |

**Post-Slice-1 cleanup**: once `src/alfred/**/*.py` lands, the `srccheck` short-circuit guards at the top of each Python gate's job should be **removed** (not toggled). A future layout-change that breaks the find pattern would otherwise silently re-enable the no-op. Tracked alongside the Task 17 (PR + CI wiring) deliverable.

## OS matrix — per-OS run/skip reality

The `ci.yml` test layer runs across three operating systems. The split below is the empirically-assessed reality (not an aspiration): each OS runs exactly what is **green** on it today, and POSIX-only tests `skipif`/are-omitted cleanly rather than turning a non-Linux leg red.

| Layer | ubuntu-latest | macos-latest | windows-latest |
|---|---|---|---|
| Ruff lint + format | ✓ (job `python`) | ✓ (job `python-cross-os`) | ✓ (job `python-cross-os`, informational) |
| mypy --strict + pyright | ✓ | ✓ (Linux-target: `--platform linux` / `--pythonplatform Linux`) | ✓ (Linux-target, informational) |
| Unit suite | ✓ (+ 100% coverage gates) | ✗ deferred (Docker-needing tests; see below) | ✗ deferred (see below) |
| Integration / Smoke (Testcontainers) | ✓ | ✗ (Linux-only — Docker daemon) | ✗ (Linux-only) |
| Real-bwrap / launcher-spawn legs | ✓ (job `integration-privileged`, under `sudo`) | n/a | n/a |

Required-vs-informational: the macOS leg is **required** (must be green); the
Windows leg is **informational-until-green** (`continue-on-error: ${{
matrix.os == 'windows-latest' }}` — visible but non-blocking). See the "Pending
required" rows above.

**Why each OS runs what it runs:**

- **ubuntu-latest** — the full battle-tested layer. The dedicated `python` job keeps its name and its per-subsystem 100% coverage gates (those need the combined unit+integration corpus the Linux `coverage-gates` job assembles, so they are deliberately NOT duplicated on the other OSes). The new `integration-privileged` job adds the root+bwrap depth (Axis 2).
- **macos-latest** — `lint + format + mypy + pyright` only. Type-checking is pinned to the Linux deployment target (`mypy --platform linux` / `pyright --pythonplatform Linux`): the runtime is Linux-only (bwrap / runuser / POSIX fds) and uses POSIX-only `os` attributes, so the right thing to type-check against is the Linux stubs the code actually runs on, on every runner OS. macOS = bash 3.2 + BSD coreutils, so this leg is the real portability signal for macOS-specific path / type drift. The unit suite is **not** run here: ~77 tests under `tests/unit/` boot a real Redis/Postgres via Testcontainers (no `@pytest.mark.integration` — they `import testcontainers` directly) and the GitHub macOS runner has no Docker daemon, so they ERROR at fixture setup (`docker.errors.DockerException`; #245 first real run = 3731 passed / 77 errored). They are not cleanly separable by a single marker or path, so a deselect allowlist would rot the moment a new Docker-backed unit test lands — the Docker-free macOS unit subset is deferred (see below).
- **windows-latest** — `lint + format + mypy + pyright` only, same layer as macOS and likewise type-checked against the Linux target (`--platform linux` / `--pythonplatform Linux`) so the runtime's POSIX-only `os` surface does not raise false `attr-defined` errors against the Windows `os` stubs. The unit suite is **not** Windows-clean: many tests invoke the POSIX bash launcher (`/bin/sh`), `os.uname()`, `os.geteuid()`, `signal.SIGKILL`, and `subprocess(..., pass_fds=...)` with no `sys.platform == "win32"` skip-guard, so they would ERROR rather than skip. Adding those guards is a cross-cutting code change across dozens of files — out of scope for this additive CI PR, tracked as a follow-up to #245. The Windows leg is informational (`continue-on-error`): visible but non-blocking until it burns in.

**Cost discipline.** A 3-OS matrix would triple minutes if applied naively. Instead: the heavy Testcontainers Integration/Smoke jobs stay Linux-only; only the cheap static-analysis layer fans out to macOS + Windows. `fail-fast: false` on the `python-cross-os` matrix so a macOS failure does not mask a Windows failure (or vice-versa).

**Deferred to a follow-up issue (#246, links #245):**

1. **Docker-free unit subset on macOS** — the ~77 Testcontainers-backed tests under `tests/unit/` need a `requires_docker` marker so the macOS leg can run `pytest tests/unit -m "not requires_docker"` and add real unit coverage on Darwin (bash 3.2 / BSD coreutils / `os.uname()`/`geteuid()` paths) without erroring on the absent Docker daemon.
2. **Unit suite on Windows** — requires `sys.platform == "win32"` skip-guards (or pure-Python reimplementations) on the bash-launcher / POSIX-syscall unit tests so they skip cleanly instead of erroring; then the Windows leg can be promoted from informational to required.
3. **macOS-native `sandbox-exec` execution** — once PR-S4-7 ships the real `sandbox-exec` invocation for `kind=full`, the macOS leg should run those sandbox legs natively. Linux already runs the native bwrap path via `integration-privileged`.

## Not currently required (but exists)

| Check name | Workflow | Why not required |
|---|---|---|
| `CodeRabbit` | (external service, no workflow file) | CodeRabbit has occasional service blips that surface as non-success status. We gate via `request_changes_workflow: true` in `.coderabbit.yaml` + the 1-approving-review rule instead, so an outage means "manually approve" rather than "repo unmergeable". |

## On bypass

Pre-push hooks (`lefthook`) can be skipped with `LEFTHOOK=0 git push`. This bypass is local-only — **the required status checks listed above cannot be bypassed**; they run on every PR and the merge button is blocked until they pass. Treat `LEFTHOOK=0` as functionally equivalent to `--no-verify`: emergency-only, never a habit.

## Audit

To compare this file against the live state:

```bash
gh api repos/alfred-os/AlfredOS/branches/main/protection \
  --jq .required_status_checks.contexts
```

The output should be a JSON array of exactly the check names in the "Currently required" table. Drift between the two is a process bug — fix the side that's wrong and open a PR.
