# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.venv/
.python-version
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# uv
.uv/

# Local environment
.env
.env.*
!.env.example

# Docker / runtime state — never commit
state/
data/
*.db
*.sqlite*

# Editor / OS
.DS_Store
.vscode/
.idea/
*.swp
*.swo
*~

# Logs
*.log
logs/

# Build artifacts
dist/
build/

# AI-tool-specific outputs are generated from .rulesync/ via `rulesync generate`.
# Source of truth lives in .rulesync/. CLAUDE.md at the repo root is the one
# exception — it stays tracked so GitHub visitors see the operating manual.
.memsearch/
.claude/
.mcp.json
# Other AI tool outputs (uncomment / add if you start generating them locally)
# .cursorrules
# .cursor/
# .clinerules
# .github/copilot-instructions.md
# AGENTS.md
# GEMINI.md
# .gemini/
